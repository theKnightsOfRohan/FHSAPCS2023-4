package ProblemSets.W11;

import java.util.ArrayList;

public class D1 {
    public static void main(String[] args) {

    }
    /*
     * public static void main(String[] args) {
     * ArrayList<Integer> list = makeRandomList(5, 100);
     * System.out.println(list);
     * leftShift(list);
     * System.out.println(list);
     * }
     * 
     * public static ArrayList<Integer> makeRandomList(int size, int max) {
     * ArrayList<Integer> list = new ArrayList<>();
     * for (int i = 0; i < size; i++) {
     * list.add((int) (Math.random() * max));
     * }
     * return list;
     * }
     * 
     * public static void removeBelow(ArrayList<Integer> arr, int min) {
     * for (int i = 0; i < arr.size(); i++) {
     * if (arr.get(i) < min) {
     * arr.remove(i);
     * i--;
     * }
     * }
     * }
     * 
     * public static void swapIndex(ArrayList arr, int i, int j) {
     * // I did it this way on purpose, to make it generic.
     * Object temp = arr.get(i);
     * arr.set(i, arr.get(j));
     * arr.set(j, temp);
     * }
     * 
     * public static void leftShift(ArrayList arr) {
     * arr.add(arr.remove(0));
     * }
     */
}
